# install all packages

install.packages(c(
  "mvtnorm", "pROC", "survey", "sandwich", "mice", "mixgb", "SuperLearner", "glmnet", "xgboost", "ranger", "optparse", "dplyr", "readr", "tmle", 
  "marginaleffects", "future", "future.apply", "dbarts"), repos = "https://cran.rstudio.com")
